/**
 * CLI 命令 - status（纯文本输出模式）
 *
 * TUI 模式由 src/tui/index.ts 的 renderTui() 处理，
 * 通过 index.ts 的 --tui 标志动态导入。
 */
export declare function statusCommand(_options?: {
    noPassword?: boolean;
}): Promise<void>;
//# sourceMappingURL=status.d.ts.map